#include <cmath>
#include <map>
#include <string>
#include <set>
#include <vector>
#include <algorithm>
#include <iostream>
#include <cmath>
#include <string>
#include <fstream> 
#include <sstream> 

#define ll long long
#define MOD 1000000007

using namespace std;

class Solver
{
public:
	ll r;
	ll c;
	Solver(){
		cin >> r >> c;
	}
	void exec(){
		int ans = 0;
		cout << ans << endl;
	}
};


int main(){
	Solver problem = Solver();
	problem.exec();

}

